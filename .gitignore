# macOS / OS
.DS_Store
Thumbs.db

# Editors / IDE
.vscode/
.idea/
*.swp

# CMake out-of-source build
build/
*.o
*.obj
*.a
*.so
*.dylib
compile_commands.json

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.python-version
.venv/
venv/

# Notebook working directory — large sequencing inputs, intermediate
# outputs, per-model test artefacts, and derived cache files are NOT
# tracked in git. (The fine-tuned basecaller checkpoints themselves live
# under the top-level models/ folder and ARE tracked.)
src/ipynb/plot/

# Sequencing & ML artefacts (belt-and-braces if staged elsewhere)
*.pod5
*.bam
*.sam
*.cram
*.fq
*.fastq
*.fq.gz
*.fastq.gz
*.npy
*.npz
*.pt
*.pth
*.onnx

# Bonito raw checkpoints — exceed GitHub's 100 MB per-file limit.
# The Dorado-ready export under models/<name>/<name>_dorado/ is what we
# ship; see models/README.md § Excluded artefacts.
models/**/weights_*.tar
